services:
  - type: web
    name: pigeon-backend
    env: python
    buildCommand: |
      echo "🚀 НАЧАЛО СБОРКИ..."
      echo "=================="
      
      # 1. Обновляем pip
      pip install --upgrade pip
      
      # 2. Устанавливаем МИНИМАЛЬНЫЕ зависимости
      echo "📦 Установка зависимостей..."
      pip install django==5.2.3
      pip install gunicorn==21.2.0
      pip install whitenoise==6.6.0
      pip install psycopg2-binary==2.9.9
      pip install dj-database-url==2.1.0
      
      # 3. ПРИНУДИТЕЛЬНО создаем таблицу
      echo "🛠️  ПРИНУДИТЕЛЬНОЕ СОЗДАНИЕ ТАБЛИЦЫ..."
      python force_create_table.py
      
      # 4. Создаем директории
      echo "📁 Создание директорий..."
      mkdir -p static/css static/js static/images
      mkdir -p staticfiles
      mkdir -p media
      mkdir -p sessions
      
      # 5. Создаем базовые статические файлы
      echo "🎨 Создание статических файлов..."
      echo "/* Базовые стили */ body { margin: 0; padding: 20px; font-family: sans-serif; }" > static/css/style.css
      echo "console.log('App loaded');" > static/js/main.js
      echo "placeholder" > static/images/Pigeon.png
      
      # 6. Пробуем миграции (на всякий случай)
      echo "🔄 Пробуем стандартные миграции..."
      python manage.py migrate auth --noinput || echo "⚠️ Миграции auth не применены"
      python manage.py migrate contenttypes --noinput || echo "⚠️ Миграции contenttypes не применены"
      python manage.py migrate sessions --noinput || echo "⚠️ Миграции sessions не применены"
      python manage.py migrate admin --noinput || echo "⚠️ Миграции admin не применены"
      
      # 7. Собираем статику
      echo "📊 Сборка статических файлов..."
      python manage.py collectstatic --noinput --clear
      
      echo "✅ СБОРКА ЗАВЕРШЕНА!"
      echo "=================="
    startCommand: gunicorn pigeon.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: pigeon-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: false
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DISABLE_COLLECTSTATIC
        value: 0
    autoDeploy: true

databases:
  - name: pigeon-db
    databaseName: pigeon
    user: pigeon_user
    plan: free